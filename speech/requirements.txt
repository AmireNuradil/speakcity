# Python 3.11 x64. Runtime dependency closure from the existing tested speech
# environment; no server, GUI, LLM, Torch, spaCy, GPU extra, scipy or soundfile.
# Some HTTP/HF packages are mandatory faster-whisper/tokenizers dependencies,
# NOT an inference service: worker.py forces offline/local-only execution.
anyio==4.15.1
attrs==26.1.0
av==18.1.0
certifi==2026.7.22
click==8.5.0
cloudpickle==3.1.2
coloredlogs==15.0.1
ctranslate2==4.6.0
dlinfo==2.0.0
espeakng-loader==0.2.4
faster-whisper==1.2.1
filelock==3.32.5
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.30.0
humanfriendly==10.0
idna==3.19
joblib==1.6.0
kokoro-onnx==0.6.1
mpmath==1.3.0
numpy==2.2.6
onnxruntime==1.23.2
packaging==26.3
phonemizer==3.4.0
protobuf==7.36.1
PyYAML==6.0.3
setuptools==84.0.0
sympy==1.14.0
tokenizers==0.23.2
tqdm==4.70.0
typing_extensions==4.16.0
# Windows-only transitives (colorama/pyreadline3) have no versions in the tested
# Linux inventory. Resolve/pin/audit those on the Windows builder before release.
# PyInstaller is a separate build tool, deliberately not a learner/runtime dep.
